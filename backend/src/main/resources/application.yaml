spring:
  application:
    name: backend

  datasource:
    url: "jdbc:mysql://localhost:3306/homefit?serverTimezone=Asia/Seoul&characterEncoding=UTF-8"
    username: "root"
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver